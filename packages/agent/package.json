{
  "name": "@citizen-0/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/cli.ts loop",
    "once": "tsx src/cli.ts once",
    "loop": "tsx src/cli.ts loop",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "@citizen-0/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
